#!/usr/bin/env bash
# Wrapper so 'latin-runs <args>' runs the script via uv from any directory.
# ${BASH_SOURCE[0]} is this file; its folder holds the script. "$@" forwards all args.
here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec uv run "$here/latin-runs.py" "$@"
